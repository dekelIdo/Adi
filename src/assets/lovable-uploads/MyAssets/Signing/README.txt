THE SIGNING EXPERIENCE — REAL SCREENSHOTS
=========================================

Both files are in place and the #signing chapter renders them.

  signing-sign.png        337x541  THE HERO. The signature screen the client
                          actually signs on: "חתימה / חתמי בתוך המסגרת", the
                          signing box, and the save-and-send action.

  signing-agreement.png   333x675  SUPPORTING. The agreement itself at the
                          signature block, with Adi's own signature.

These are the owner's real screenshots. The duplicates that briefly lived in
MyAssets/AdiArieli/ have been removed - these two files are the only copies.

Notes
-----
* These are 1x phone screenshots (337px and 333px wide). The layout caps them at
  their native width so they are never upscaled and never soften. That is also
  why the hero plate stops growing past 337px on a desktop: bigger would mean
  blurrier, and a crisp small screenshot reads as real while a soft large one
  reads as a mockup.
* Nothing is cropped and nothing is stretched - measured distortion is 0 at
  every width. The plates take the screenshots' true shape.
* The personal details visible in the agreement are Adi's own business email and
  phone, which the site already publishes. No third-party data appears.
* There is no webp <source>: it would win over the PNG in every browser that
  supports it, and a missing webp would take the whole chapter down.

THE PHOTOGRAPH AND ITS DOCUMENT LAYER
=====================================

  signing-adi.jpg / .webp            1400x2096  The real photograph of Adi writing on
                                     the clipboard. Never masked, warped or redrawn.

  signing-adi-agreement-layer.png    1400x2096  A transparent layer with the photo's
                                     exact dimensions: the agreement's signature
                                     block (pixels from signing-agreement.png),
                                     perspective-mapped onto the sheet on the
                                     clipboard and shaded by the sheet's own light.
                                     It is keyed to the sheet's paper, so her hands,
                                     nails, the pen and the clip are transparent in
                                     it and stay in front. Rendered in the same box
                                     with the same fit and crop as the photograph,
                                     which is what keeps it pixel-locked at every
                                     width. Regenerate it from the photo if the
                                     agreement changes; never from an AI render.

Not in use, and not to be used: the AI-reconstructed renders of Adi holding a glowing
signing screen (adi-signing-*, adi-arieli-private-sign-*, Adi_sign_final_image_to_prod).
Their hands, jeans and proportions are synthetic. The chapter is built on the real photo.
